apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "fullname" . }}
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
spec:
  rules:
  - host: {{ .Release.Name }}.{{ .Values.expose.config.domain }}
    http:
      paths:
      - path: /
        backend:
          serviceName: hello-world
          servicePort: {{ .Values.service.externalPort }}